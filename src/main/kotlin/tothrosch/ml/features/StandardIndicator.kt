package tothrosch.ml.features

// indicator with a max size, you add the number at the end and it removes the first one
/*class StandardIndicator<T>(val maxSizeQueue: MaxSizeQueue<T> = MaxSizeArrayDeque(Settings.indicatorLength)) :
	MaxSizeQueue<T> by maxSizeQueue, Indicator {
	override fun copy(): StandardIndicator<T> = StandardIndicator(maxSizeQueue.copy())
}*/

