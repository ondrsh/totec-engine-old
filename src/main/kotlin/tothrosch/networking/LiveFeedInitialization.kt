package tothrosch.networking

/**
 * Created by ndrsh on 21.06.17.
 */

/*
interface LiveFeedInitialization    {
    val booksInitialization: HashMap<String, Initialization>

    suspend fun initialize(msgSequence: Long, node: JsonNode, init: Initialization, channel: SendChannel<Message>)
    suspend fun processFullBook(node: JsonNode, sym: String)
}*/
